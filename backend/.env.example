# Copy this file to .env for local overrides. Never commit a real .env
# (Norma 11, Seguridad por Diseño — 000-Constitucion/11-Seguridad.md).

APP_NAME="MERCHLY AI Backend"
APP_VERSION="0.1.0"
ENVIRONMENT=local

DATABASE_URL=postgresql://merchly:merchly@db:5432/merchly
REDIS_URL=redis://redis:6379/0

# Gemini (Agente Investigador de Producto — ver docs/007-Agentes y
# docs/010-Prompts). Dejar vacío para seguir usando el proveedor
# simulado; completar solo en un .env real, nunca aquí.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
